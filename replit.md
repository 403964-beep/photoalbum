# Replit setup

This is a Vite + React photo gallery. The preview workflow runs the existing
development server with:

```bash
npm run dev
```

The server listens on `0.0.0.0:5000`, which is the port used by the Replit
web preview.